#include<stdio.h>
int sum(int,int);
int main()
{
    int sum;
    int s;
int (*p)(int,int)=sum&sum;
s=(*p)(5,3);
printf("%d",sum(a,b));
}
int sum(int a,int b)

{
int a,b;
int sum(int a,int b);

    return a+b;
}
